[project]
name = "bbsplus"
version = "0.1.0"
dependencies = ["py-ecc>=8"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
